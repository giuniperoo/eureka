section.font-container
  .title fonts

  ul.font-list
    li.sans{action 'selectFont' 'sans' target='view'}
      .radio-select
      | sans
      input type='text' placeholder='The quick brown fox jumps over the lazy dog'
    li.mono{action 'selectFont' 'mono' target='view'}
      .radio-select
      | mono
      input type='text' placeholder='The quick brown fox jumps over the lazy dog'
    li.cursive{action 'selectFont' 'cursive' target='view'}
      .radio-select
      | cursive
      input type='text' placeholder=' The quick brown fox jumps over the lazy dog'

section.emoji-container
  .title emoji

  .emoji-toggle-container
    .status disabled
    = input#emoji-toggle.toggle type='checkbox' checked=emojiActive
    label for='emoji-toggle'

  img.lazy data-original="<%= image_path('emoji-dropdown.gif') %>"

  p Just type a colon (:) to trigger the emoji dropdown. The selected emoji will appear in the resulting maptile.
